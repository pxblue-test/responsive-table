version: 2
jobs:
  build:
    working_directory: ~/responsive-table
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: 
          name: Install dependencies
          command: yarn 
      - run:
          name: Test React 
          command: yarn test